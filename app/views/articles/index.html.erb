<div class="container">
  <div class="articles margin-top-medium">
    <ul>
      <% @articles.each do |article| %>
      <h2><%= link_to article.title, article_path(article) %></h2>
      <p><%= article.content %></p>
      <% end %>
    </ul>
  </div>
</div>

